class EventsHandler {
    constructor() { //add used classes in ()

    }

    pickNewChellenge () {
        $('.challenge').on('click', ()=> {
            //let = get $this.data('type').val()
            //move to route /startchallenge: challenge explnation and a button: startNewChallenge()
        })
    }

}

export default EventsHandler